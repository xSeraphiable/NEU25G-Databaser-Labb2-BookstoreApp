<UserControl x:Class="BookstoreApp.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookstoreApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >

    <Menu>
        <MenuItem Header="Start">
            <MenuItem Header="Byt butik"
                      ItemsSource="{Binding Stores}">
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="MenuItem">
                        <Setter Property="Header" 
                                Value="{Binding Street}" />
                        <Setter Property="Command"
                                Value="{Binding DataContext.SelectStoreCommand,
                                                RelativeSource={RelativeSource AncestorType=Menu}}" />
                        <Setter Property="CommandParameter" Value="{Binding}" />
                    </Style>
                </MenuItem.ItemContainerStyle>

            </MenuItem>

            <MenuItem Header="Avsluta"/>
        </MenuItem>
        <MenuItem Header="Register">
            <MenuItem Header="Lagersaldo" Command="{Binding ShowStockCommand}"/>
            <MenuItem Header="Böcker" Command="{Binding ShowBooksCommand}"/>
            <MenuItem Header="Författare" Command="{Binding ShowAuthorsCommand}"/>
        </MenuItem>
        <MenuItem Header="Redigera"/>
        <MenuItem Header="Hjälp"/>
    </Menu>
</UserControl>
