<Window x:Class="BookstoreApp.Views.SelectStoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookstoreApp.Views"
        mc:Ignorable="d"
        Title="Inloggning"   
        SizeToContent="Height"
        Width="350"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <StackPanel Margin="16">
        <TextBlock Text="Välkommen till Bokhandel admin"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"/>
        <TextBlock Text="Välj butik:"
                   
                   
                   Margin="0,0,0,12"/>

        <ComboBox ItemsSource="{Binding Stores}"
                  SelectedItem="{Binding SelectedStore}"
                  DisplayMemberPath="Street"
                  Margin="0,0,0,16"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="80"
                    IsDefault="True"
                    IsEnabled="{Binding HasSelectedStore}"
                    Click="OK_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
